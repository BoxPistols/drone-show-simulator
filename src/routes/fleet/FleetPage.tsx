/**
 * Placeholder for Fleet operations. P5 will port fleet.jsx into typed
 * components (FleetGrid / FleetTable / DroneDrawer / FormationAssignPreview).
 */
export function FleetPage() {
  return (
    <div style={{ flex: 1, padding: 32, color: 'var(--text-1, rgba(255,255,255,0.78))' }}>
      <h1
        style={{
          fontFamily: 'Shippori Mincho, serif',
          fontSize: 26,
          margin: 0,
          letterSpacing: '0.08em',
        }}
      >
        機体管理
      </h1>
      <p style={{ opacity: 0.6, marginTop: 12 }}>Fleet — implementation lands in P5</p>
    </div>
  );
}
